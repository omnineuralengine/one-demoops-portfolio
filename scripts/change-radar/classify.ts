const HIGH = /\b(deprecat(?:ed|ion)|retire(?:d|ment)|removed|breaking change|required migration|changed default|permission|role|entitlement|authentication|credential|security|compliance|data retention|rate limit|spend limit|billing|model id|api field|beta header|connector|tool access)\b/i;
export function classifyDeterministically(text: string) { const high = HIGH.test(text); return { impactLevel: high ? "REVIEW_REQUIRED" as const : "LOW" as const, confidence: high ? 0.92 : 0.7, inference: true as const }; }
export function parsePublishedAt(value: string): string | null { const match = value.match(/\b(20\d{2}-\d{2}-\d{2})\b|\b(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\s+\d{1,2},\s+20\d{2}\b/i); if (!match) return null; const date = new Date(`${match[0]} UTC`); return Number.isFinite(date.valueOf()) ? date.toISOString() : null; }
